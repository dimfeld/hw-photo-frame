HOST=0.0.0.0
PORT=3000
ORIGIN=http://localhost:3000
PHOTO_DATA_DIR=./data
# No application upload cap. Set this to suit the memory on your Linux server.
BODY_SIZE_LIMIT=Infinity
# Optional shared token for /frame requests. Set the same token in secrets.h.
FRAME_TOKEN=
